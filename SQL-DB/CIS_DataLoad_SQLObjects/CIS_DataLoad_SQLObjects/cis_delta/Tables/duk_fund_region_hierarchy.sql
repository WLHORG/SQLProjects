CREATE TABLE [cis_delta].[duk_fund_region_hierarchy] (
    [branch]      VARCHAR (4)  NOT NULL,
    [rf_area]     VARCHAR (10) NOT NULL,
    [fund_region] VARCHAR (10) NOT NULL,
    PRIMARY KEY CLUSTERED ([branch] ASC)
);

